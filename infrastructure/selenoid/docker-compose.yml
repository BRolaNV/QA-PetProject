services:
  selenoid:
    image: aerokube/selenoid:latest
    container_name: selenoid
    network_mode: bridge
    ports:
      - "4445:4444"
    environment:
      - DOCKER_API_VERSION=1.40
      - OVERRIDE_VIDEO_OUTPUT_DIR=/c/Users/user/IdeaProjects/QA-PetProject/infrastructure/selenoid/video
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./:/etc/selenoid/:ro
      - ./video:/opt/selenoid/video
    command: ["-limit", "3"]

  selenoid-ui:
    image: aerokube/selenoid-ui:latest
    container_name: selenoid-ui
    network_mode: bridge
    links:
      - selenoid
    ports:
      - "8081:8080"
    command: [ "--selenoid-uri", "http://selenoid:4444" ]